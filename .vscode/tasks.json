{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile to .vex",
            "type": "shell",
            //"command": "./.vscode/compileVFL.sh",
            //"command": "vcc '$(wslpath '${file}')' && ./a.out"
            
        
        }
    ]
}